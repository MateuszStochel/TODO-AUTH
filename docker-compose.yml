version: '3'
services:
  frontend:
    build:
      context: ./front-end
    ports:
      - "3000:3000"
    depends_on:
      - backend
  backend:
    build:
      context: ./server
    ports:
      - '6000:6000'